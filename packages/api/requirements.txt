fastapi>=0.115.0
uvicorn[standard]>=0.32.0
aiosqlite>=0.20.0
pydantic>=2.10.0
yt-dlp>=2024.12.0
openai>=1.50.0
httpx>=0.27.0
python-dotenv>=1.0.0
python-multipart>=0.0.12

# YouTube PO Token auto-authentication (zero user interaction)
bgutil-ytdlp-pot-provider>=0.7.0

# SAM-Audio — soundfile is always needed; torch/torchaudio only for local mode
soundfile>=0.12.0

# Local inference (optional — install if not using RunPod)
# torch>=2.1.0
# torchaudio>=2.1.0
# sam-audio-infer>=0.1.0
