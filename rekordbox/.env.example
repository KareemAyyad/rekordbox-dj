## DropCrate environment

# ─────────────────────────────────────────────────────────────────────────────
# OPENAI (Auto-classification)
# ─────────────────────────────────────────────────────────────────────────────
# Optional: enables higher-quality LLM-based auto-tagging in browser mode
OPENAI_API_KEY=

# Optional: override OpenAI model (defaults to gpt-5.2)
# DROPCRATE_OPENAI_MODEL=gpt-5.2

# ─────────────────────────────────────────────────────────────────────────────
# ACOUSTID + MUSICBRAINZ (Fingerprint matching)
# ─────────────────────────────────────────────────────────────────────────────
# Optional: fingerprint + authoritative metadata match (AcoustID -> MusicBrainz).
# If set, DropCrate will attempt to replace Artist/Title/Album/Year when match score is high.
# Get your API key at: https://acoustid.org/my-applications
DROPCRATE_ACOUSTID_KEY=

# Optional: path to `fpcalc` (Chromaprint). If not set, DropCrate will try `fpcalc` from PATH.
# Install with: brew install chromaprint (macOS) or apt install libchromaprint-tools (Linux)
DROPCRATE_FPCALC_PATH=

# Optional: MusicBrainz requires a User-Agent string. Default is "DropCrate/0.1.0 (local)".
DROPCRATE_MUSICBRAINZ_UA=

# ─────────────────────────────────────────────────────────────────────────────
# YT-DLP (YouTube downloader)
# ─────────────────────────────────────────────────────────────────────────────
# Optional: path to custom yt-dlp binary. If not set, DropCrate will auto-download.
DROPCRATE_YTDLP_PATH=

# Optional: authenticate with YouTube using browser cookies.
# Useful for age-restricted content or avoiding rate limits.
# Values: chrome, firefox, edge, safari, opera, brave, chromium, vivaldi
DROPCRATE_COOKIES_FROM_BROWSER=

# Optional: path to a Netscape-format cookies file (alternative to browser cookies)
DROPCRATE_YTDLP_COOKIES=

# ─────────────────────────────────────────────────────────────────────────────
# BRIDGE SERVER
# ─────────────────────────────────────────────────────────────────────────────
# Optional: override bridge server port (defaults to 8787)
DROPCRATE_BRIDGE_PORT=8787
